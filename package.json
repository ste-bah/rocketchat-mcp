{
  "name": "rocketchat-mcp",
  "version": "1.0.0",
  "description": "MCP server for RocketChat — enables AI agents to send/receive messages, read channels, and DM users",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "npx tsx src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
